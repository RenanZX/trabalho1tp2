class ValidarAdmin{
public:
  validar() throw(runtime_error);

}

class GerenciadorDeTopicos{

private:
  void incluir() throw(runtime_error);
  void remover() throw(runtime_error);
  void editar() throw(runtime_error);
}
