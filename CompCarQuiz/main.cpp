#include <iostream>

using namespace std;

int main()
{
    /*int pos = 0;
    string dado = "abc|def|23|";
    string dat1,dat2,dat3;

    dat1 = dado.substr(0,dado.find("|"));
    pos = dado.find("|")+1;
    dado = dado.substr(pos,dado.length());
    dat2 = dado.substr(0,dado.find("|"));
    pos = dado.find("|")+1;
    dado = dado.substr(pos,dado.length());
    dat3 = dado.substr(0,dado.find("|"));
    cout << dat1 << endl;
    cout << dat2 << endl;
    cout << dat3 << endl;*/

    return 0;
}
